"""Synthetic fixtures.

Everything here is generated, deterministic, and tiny. It mimics the *shape* of
PixelRec50K so the pipeline can be exercised offline; it is never presented as
real project data, and no number produced from it appears in a report.
"""
